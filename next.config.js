/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: {
      domains: ['raw.githubusercontent.com'],
    },
    env: {
      customKey: 'my-value',
    },
  };
